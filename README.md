# 気温・湿度・絶対湿度

現在地の気温・相対湿度・**絶対湿度**を、実況と予報で表示するアプリです。
ビルド不要の静的ファイル（HTML／CSS／JavaScript）だけでできています。

**https://hironishimura.github.io/weather-absolute-humidity/**

相対湿度だけでは空気の乾き具合が分かりにくいため、
容積絶対湿度（g/m³）を主役に据えています。結露やカビ、加湿・除湿の判断に使えます。

## 表示するもの

- **気温**／**相対湿度**／**容積絶対湿度（g/m³）** … 大きく3つ
- 重量絶対湿度（g/kg(DA)）・露点温度・現地気圧・風・前1時間降水量・不快指数
- **結露チェック** … 窓や壁の表面温度を入れると、露点と比べて結露するかどうかを判定します
- 提供元ごとの値を並べた表
- 時系列グラフ（実況の過去24時間／これからの予報）— 気温・相対湿度・絶対湿度の3段
- 気象庁の日別予報（3日＋週間）と気象概況

地点はブラウザに覚えさせるので、次に開いたときは同じ場所から始まります。

## 開き方

### そのまま使う

上のURLを開くだけです。スマホのホーム画面に追加しておくと便利です。

### 1ファイル版（ネットの置き場所なしで使う）

`dist/weather-app.html` をダウンロードしてダブルクリックするだけです。
作り直すときは `python3 scripts/build_single.py`。

ただし `file://` で開くとブラウザの決まりで**現在地の取得が使えません**。
画面の「地点を手で設定する」から緯度・経度を入れてください（既定は宇都宮）。

### 手元でサーバを立てる

```bash
php -S 127.0.0.1:8080 -t docs
python3 -m http.server 8080 --directory docs
```

→ http://127.0.0.1:8080/

現在地の取得（Geolocation）は **https か localhost** でしか動きません。

## 中身

```
docs/          アプリ本体。GitHub Pages が公開しているのはここです
  index.html
  assets/      app.css / app.js
  data/        取り込みスクリプトが latest.json を置く場所
dist/          1ファイルにまとめた版
scripts/       取り込みと1ファイル化のスクリプト
test/          計算と読み取りの確認
```

## データの出どころ

| 提供元 | 内容 | 取り方 | 既定 |
|---|---|---|---|
| 気象庁 | アメダス実況（気温・湿度・気圧・風・降水）、過去24時間の10分値 | ブラウザから直接 | 有効 |
| 気象庁 | 府県天気予報（3日・週間の天気／気温／降水確率／気象概況） | ブラウザから直接 | 有効 |
| 気象庁MSM・GSM（Open-Meteo経由） | 1時間ごとの気温・湿度の予報 | ブラウザから直接 | 有効 |
| Yahoo!天気 | 1時間ごとの気温・湿度 | サーバ側で取り込み | **無効** |
| ウェザーニュース | 1時間ごとの気温・湿度 | サーバ側で取り込み | **無効** |

### なぜ Open-Meteo を使っているか

気象庁が公開している府県天気予報には**湿度が入っていません**。
そのため、絶対湿度の「予報」を出すには数値予報モデルの値が要ります。
Open-Meteo は気象庁の数値予報（MSM・GSM）をそのまま配信していて、
APIキーなしで、ブラウザから直接読める形（CORS対応）で提供しています。
つまり中身は気象庁のデータで、受け取り口だけを借りている形です。

### Yahoo!天気とウェザーニュースが既定で無効な理由

どちらも一般に公開されたAPIがなく、**ブラウザから直接は読めません**（CORSで止まります）。
読むにはサーバ側でページを取得して解析する必要があります。そのため、

- ページの作りが変わると読めなくなります（壊れやすい）
- 各社の利用規約の確認が要ります

という事情があり、既定では止めてあります。
気象庁と数値予報だけでもアプリは問題なく使えます。

## Yahoo!天気・ウェザーニュースを使うには

1. それぞれのサイトで自分の地域のページを開き、そのURLを控える
2. `scripts/settings.json` の `url` を書き換え、`enabled` を `true` にする
3. 利用規約を確認する
4. 動かす

```bash
# 書き出さずに読めるかどうかだけ試す
python3 scripts/collect.py --dry-run

# 保存したページで読み取りを試す
python3 scripts/collect.py --html yahoo=saved.html --dry-run

# 本番（docs/data/latest.json に書き出す）
python3 scripts/collect.py
```

定期的に取り込むなら `.github/workflows/weather-collect.yml` の
`schedule` のコメントを外してください（既定は3時間おき）。
手元の cron から `collect.py` を叩くだけでも構いません。

読めなかったときは `latest.json` に理由が残り、アプリの「取得状況」に**失敗**と出ます。

## 計算式

| 量 | 式 |
|---|---|
| 飽和水蒸気圧 | `es = 6.1078 × 10^(7.5t/(t+237.3))` [hPa]（Tetens） |
| 水蒸気圧 | `e = es × RH/100` [hPa] |
| 容積絶対湿度 | `216.68 × e /(t+273.15)` [g/m³] |
| 重量絶対湿度 | `621.945 × e /(P−e)` [g/kg(DA)] |
| 露点温度 | Magnusの式 |
| 不快指数 | `0.81t + 0.01RH(0.99t−14.3) + 46.3` |

気圧 `P` は観測値があればそれを、なければ観測所の標高から推定した値を使います。

## 確認

```bash
node test/run.js                  # 計算・読み取り・予報JSONの解釈
python3 scripts/test_collect.py   # 取り込みスクリプトの読み取り処理
```

ネットワークにはつながず、モックしたデータで確かめます。

## 注意

- 表示する値は目安です。設計や気密測定の判断に使うときは実測値と照らし合わせてください。
- 気象庁のアメダスは全地点が湿度を測っているわけではありません。
  アプリは**気温と湿度がそろっている一番近い観測所**を選び、その距離を画面に出します。
